#define MAX(A,B) (((A)>=(B)) ? (A) : (B))
#define MIN(A,B) (((A)<=(B)) ? (A) : (B))
#define MAX_COMMAND 900
#define MAX_PATH_FILE_NAME 500
#define MAX_FILE_NAME 200
#define MAX_PATH 300
#define MAX_LINE_FILE 500
