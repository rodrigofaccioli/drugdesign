#define MAX(A,B) (((A)>=(B)) ? (A) : (B))
#define MIN(A,B) (((A)<=(B)) ? (A) : (B))
#define MAX_COMMAND 1000
#define MAX_PATH_FILE_NAME 600
#define MAX_FILE_NAME 300
#define MAX_PATH 300
#define MAX_LINE_FILE 500
#define MAX_SUFIX 10